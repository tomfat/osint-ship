# Supabase project configuration
# Rename this file to `.env.local` and fill in the credentials before running the app locally.
NEXT_PUBLIC_SUPABASE_URL="https://your-project-ref.supabase.co"
# Use the service role key for server-side access in API routes only. Do NOT expose this to the browser.
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"
